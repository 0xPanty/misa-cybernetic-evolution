# External Trajectory Pro Review Bundle 2026-05-16

This directory contains the sanitized local evidence bundle for reviewing the
external trajectory optimization lane.

## Anchors

```text
optimization-before GitHub baseline:
  ref=origin/codex/local-vector-store-adapter
  commit=3e79083

optimized local branch:
  ref=codex/local-vector-store-adapter
  commit=1bfd0ac8fc8945b8304f2e8c5f6a3d8fe966666b

optimized behavior anchor used by the final comparison:
  commit=1bfd0ac8fc8945b8304f2e8c5f6a3d8fe966666b
  branch_tip_aligned=true
```

## Files

```text
external-trajectory-final-comparison.json
  Formal full-batch baseline-vs-optimized comparison generated by:
  npm run external:compare

external-trajectory-final-comparison.md
  Markdown summary of the same branch-tip comparison.

external-trajectory-side-by-side.json
  867 sanitized comparison records with baseline action/score and optimized
  calibrated action/score.

external-trajectory-alpha.json
  Alpha analysis, Qianxuesen second-order fit, ablation/readout channels, and
  shadow policy surface.
```

## Main Result

```text
samples=867
baseline_avg_score=0.723
optimized_avg_score=0.809
avg_delta=+0.086
baseline_expected_match_rate=0.743
optimized_expected_match_rate=1.000
expected_match_lift=+0.257
regression_count=0
safety_regressions=0
holdout_passed=true
baseline_to_optimized_action_change_count=223
shadow_readout_action_change_count=0
route_authority_changed=false
winner_authority_changed=false
production_authority=false
```

## Action vs Score Split

```text
action_change_count=223
action_improvement_count=223
action_regression_count=0
unchanged_action_count=644

action_change_avg_delta=+0.131
same_action_avg_delta=+0.071
action_change_delta_share=0.389
same_action_delta_share=0.611
```

Interpretation:

```text
The behavior-level gain is real: 223 samples changed to the expected action and
none changed away from it.

The score-level gain is mixed: 61.1% of total score lift comes from samples whose
action did not change. That same-action lift is useful calibration evidence, but
it should not be reported as behavior change.
```

## Grouped Holdout

```text
mode=grouped_holdout_over_sanitized_batch
independence_level=stronger_than_hash_split_but_not_external_holdout
conclusion=grouped_holdout_passed_without_regression

dataset groups: 6/6 passed
expected_shadow_action groups: 5/5 passed
issue_kind groups with min_count=5: 10/10 passed
```

Interpretation:

```text
This closes the Pro review request for a harder grouped check over the available
sanitized batch. It is stronger than the old hash split, but it is still not a
fresh external holdout from new data.
```

## Commit Note

```text
The comparison code/behavior anchor is 1bfd0ac8fc8945b8304f2e8c5f6a3d8fe966666b.
Any later commit that only refreshes this review bundle should be read as report
packaging, not as a behavior change to the side-by-side calibration.
```

## Boundary

```text
shadow_only=true
raw_external_content_persisted=false
persistent_memory_written=false
zilliz_written=false
embedding_created=false
llm_api_calls=false
external_api_calls=false
vps_touched=false
github_pushed=false
```

## Review Notes

The GitHub baseline commit predates the external-trajectory harness. The final
comparison therefore uses the current harness as a neutral measurement layer:
baseline action/score represents pre-optimization behavior, and calibrated
action/score represents optimized behavior on the same sanitized 867-sample
batch.

The intended review question is not whether this has production authority. It
does not. The intended question is whether the local shadow-only optimization is
positive, stable, and worth keeping as the GitHub branch state.

## Pro Follow-up Closure

Handled in this bundle:

```text
1. Branch-tip comparison is now aligned to 1bfd0ac8fc8945b8304f2e8c5f6a3d8fe966666b.
2. Action-level improvement and score-level lift are reported separately.
3. Grouped holdout was added for dataset, expected action, and issue kind.
4. Expected-match rate is kept as policy-conformance evidence, not treated as
   the only victory metric.
5. Alpha analysis remains research/support evidence; no new alpha gate,
   route authority, winner authority, memory write, Zilliz write, embedding, or
   live LLM call was added.
```

Still future work:

```text
1. Fresh larger external samples.
2. More independent grouped holdout by source project, repo, time, and task
   family when those fields exist.
3. Online observe-only shadow validation after full perception is available.
```
